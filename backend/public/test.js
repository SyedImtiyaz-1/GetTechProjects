import baseURL from './config.js';

// Use the baseURL in your code
console.log(baseURL);

// You can also use it to construct complete URLs
const projectURL = `${baseURL}/project-details`;
console.log(projectURL);